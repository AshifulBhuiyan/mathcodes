export default {
  title: "Understanding Limits (WIP)",
  description: "An intuitive intro to limits with code examples",
  content: `
<h1>Understanding Limits</h1>

\\[
\\lim_{x \\to c} f(x) = L
\\]

Let’s start with a simple function...

<pre><code class="language-js">
function f(x) {
  return (x ** 2 - 1) / (x - 1);
}
</code></pre>

What happens as \`x\` gets closer to 1?
`
};
